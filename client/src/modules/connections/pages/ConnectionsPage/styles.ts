import Box from '@mui/material/Box'
import Stack from '@mui/material/Stack'
import { styled } from '@mui/material/styles'


export const Alert = styled(Box)({
  marginBottom: 18,
})

export const Cards = styled(Stack)({
  gap: 14,
})
